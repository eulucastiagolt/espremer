'use client';

import { useSearchParams, useRouter } from 'next/navigation';
import { Suspense, useState } from 'react';

function ActivateForm() { const params = useSearchParams(); const router = useRouter(); const [password, setPassword] = useState(''); const [confirmPassword, setConfirmPassword] = useState(''); const [error, setError] = useState(''); const submit = async (event: React.FormEvent) => { event.preventDefault(); const response = await fetch('/api/admin/activate', { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify({ token: params.get('token'), password, confirmPassword }) }); const data = await response.json(); if (!response.ok) setError(data.error); else router.push('/admin/login'); }; return <main className="min-h-screen flex items-center justify-center bg-zinc-100 dark:bg-zinc-950 px-4"><form onSubmit={submit} className="w-full max-w-sm bg-white dark:bg-zinc-900 rounded-xl p-6 space-y-3"><h1 className="text-lg font-semibold text-zinc-900 dark:text-white">Defina sua senha</h1><input required type="password" minLength={8} value={password} onChange={(e) => setPassword(e.target.value)} placeholder="Nova senha" className="w-full px-3 py-2 rounded-lg bg-zinc-100 dark:bg-zinc-800" /><input required type="password" minLength={8} value={confirmPassword} onChange={(e) => setConfirmPassword(e.target.value)} placeholder="Confirmar senha" className="w-full px-3 py-2 rounded-lg bg-zinc-100 dark:bg-zinc-800" /><button className="w-full py-2 bg-blue-500 text-white rounded-lg">Ativar acesso</button>{error && <p className="text-sm text-red-600">{error}</p>}</form></main>; }
export default function ActivatePage() { return <Suspense><ActivateForm /></Suspense>; }
